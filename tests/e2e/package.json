{
  "name": "hyprpilot-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright e2e suite driving the hyprpilot Tauri webview via @srsholmes/tauri-playwright.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "format": "prettier --write . --log-level warn && eslint --fix",
    "lint": "eslint"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@srsholmes/tauri-playwright": "^0.2.2"
  }
}
