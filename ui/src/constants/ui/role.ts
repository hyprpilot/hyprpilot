/**
 * Captain (`User`) / Pilot (`Assistant`) — the chat speaker
 * discriminator. Drives the role-stripe colour + role tag rendering.
 */
export enum Role {
  User = 'user',
  Assistant = 'assistant'
}
