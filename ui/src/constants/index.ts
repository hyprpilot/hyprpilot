/**
 * Top-level barrel for enums + constant tables. Mirrors `interfaces/`
 * with `wire/` (closed-set wire discriminators) and `ui/` (UI-only
 * enums + their tone/icon/css mappers).
 */
export * from './wire'
export * from './ui'
