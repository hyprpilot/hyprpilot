/**
 * codex-acp's per-tool presentation overrides. Empty for now —
 * every codex tool falls through to the kind defaults until we
 * read real sessions and learn its wire-name conventions.
 */
import type { Presentation } from '@lib/tools/presentation'

export const codexOverrides: Record<string, Presentation> = {}
