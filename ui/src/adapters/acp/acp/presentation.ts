/**
 * Generic ACP-binary presentation overrides. Empty — user-supplied
 * ACP binaries inherit every kind default with no UI-side overrides.
 */
import type { Presentation } from '@lib'

export const acpOverrides: Record<string, Presentation> = {}
