// shadcn-vue primitives import `cn` from `@components/lib/utils`. Our
// project's `cn` lives at `@lib`; this thin re-export keeps the
// shadcn imports valid without rewriting every primitive.
export { cn } from '@lib'
