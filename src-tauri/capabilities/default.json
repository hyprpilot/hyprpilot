{
  "identifier": "default",
  "description": "Baseline capability for the main webview — core invoke + event surface required for useAcpAgent / useTheme / useWindow bindings.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "log:default",
    "clipboard-manager:allow-read-image",
    "clipboard-manager:allow-read-text"
  ]
}
