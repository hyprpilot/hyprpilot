//! ACP transport adapter — placeholder for Pass 1.
//!
//! The real content lands in Pass 2 (K-251): every file under
//! `src-tauri/src/acp/` relocates here via `git mv`, types rename
//! (`AcpSessions` → `AcpInstances`, `SessionKey` → `InstanceKey`,
//! …), and `impl Adapter for AcpAdapter` wires the ACP wire shape
//! into the generic `Adapter` surface.
